<h1>Super To-Do List!</h1>
</br />
<%= link_to 'New List', new_list_path %>
<% if @lists.any? %>
  <ul>
    <% @lists.each do |list| %>
      <li>
      <%= link_to list.title, list_path(list) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>No lists right now. Please make a list and start doing!</h3>
<% end %>